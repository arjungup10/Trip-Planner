<menu xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/create_trips_menu">

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/save"
        android:title="Save">
    </item>
</menu>